# 游戏管理器环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# API 配置
SERPAPI_KEY=your_serpapi_key_here
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_CX=your_google_custom_search_engine_id_here

# 代理配置
USE_PROXY=false
PROXY_HOST=127.0.0.1
PROXY_PORT=7890

# 白名单配置
STRICT_WHITELIST=false  # true=严格白名单模式，false=智能验证模式

# 使用说明：
# 1. 如果你有代理服务器，设置 USE_PROXY=true 并配置 PROXY_HOST 和 PROXY_PORT
# 2. 如果不需要代理，保持 USE_PROXY=false
# 3. API 密钥是可选的，不配置会使用基础爬虫功能

# Python代理设置方法（类似Java的-Dhttps.proxyHost）：
# 方法1：环境变量（推荐）
#   export USE_PROXY=true
#   export PROXY_HOST=127.0.0.1
#   export PROXY_PORT=7890
#   python scripts/game_manager.py
#
# 方法2：使用代理工具
#   python scripts/set_proxy.py set 127.0.0.1 7890
#   python scripts/game_manager.py
#
# 方法3：命令行参数
#   python scripts/game_manager.py --use-proxy 